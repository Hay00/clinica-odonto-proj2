import { createMuiTheme } from '@material-ui/core/styles';

export const defaultTheme = createMuiTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#26a69a',
    },
    secondary: {
      main: '#008ac0',
    },
  },
});
